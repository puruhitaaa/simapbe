{
  "name": "@simapbe/auth",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "auth:generate": "bun x @better-auth/cli generate --config=./src/index.ts"
  },
  "dependencies": {
    "@simapbe/db": "workspace:*",
    "@simapbe/env": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@simapbe/config": "workspace:*",
    "typescript": "catalog:"
  }
}
